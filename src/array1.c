#include<stdio.h>
int main ()
{
    int num[] = {10,25,87,92};              // declaração de array(conjunto de dados) que guardo quatro numeros inteiros
    int pos = 6;                            //pos para lembrar posição
    printf("%d\n" ,num[pos -5]);            // mostra a primeira posição do array

    return 0;

}