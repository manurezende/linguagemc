// importando a biblioteca padrao de entrada e saida de dados
// std(standard) I(input) o(out)
#include <stdio.h>  
// inicio do programa
int main () {
// comando para exebir o texto na tela
// o comando só executa por que você fez a importação da biblioteca stdio
    printf("hello world\n");

    // o comando abaixo informa ao compilador que o programa acabou
    return 0;

}